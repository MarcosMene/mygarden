{% extends 'base.html.twig' %}
{% block title %}My Garden -
	{{ form.vars.value.id ? 'Edit Carrier' : 'Create New Carrier' }}
{% endblock %}

{% block body %}

	<!-- MENU  -->
	{% include ('admin/components/titleDashboardAdmin.html.twig') %}
	<div class="back_to text-end px-8 lg:px-16">
		<a href="{{path('show_carriers')}}" class="text-gray underline text-base  ">Back to carriers</a>
	</div>
	<div
		class="create_carriers w-full flex px-8 lg:px-16 mb-10">

		<!-- MENU LEFT  -->
		<div class="menu_admin  flex flex-col w-1/3 md:w-1/6 p-2 lg:p-8 bg-gray-50">
			<div class="sticky top-0">
				{% include ('admin/_partials/menu_navigation.html.twig')%}
			</div>
		</div>
		<!-- RIGHT CREATE FORM -->
		<aside class="w-2/3 md:w-5/6 bg-white border border-gray p-2 lg:p-8 h-fit">
			<div class="title_form flex flex-wrap justify-between">
				<h2 class=" text-black font-medium text-xl mb-4">
					{{ form.vars.value.id ? 'Edit Carrier' : 'Create Carrier' }}
				</h2>
			</div>

			<!--ERROR MESSAGE-->
			<div id="error_messages" class="hidden text-center p-4 mb-4 text-sm rounded-lg bg-red-100 text-red-700 relative" role="alert"></div>

			{{ form_start(form) }}

			<!-- NAME -->
			<div class="flex flex-col md:flex-row justify-between mb-2">
				<div class=" w-full  text-base">
					{{ form_label(form.name) }}
					{{ form_widget(form.name, {
        'attr': {
            'class': (attr.class|default('w-full rounded bg-white p-3 shadow shadow-gray-100 mt-2 appearance-none outline-none py-2 px-3 leading-4 text-black') ~ (form_errors(form.name) ? ' is-invalid' : ''))
        }
    }) }}
					<div class="form-error">
						{{ form_errors(form.name) }}
						<div id="carrierName_error" hidden class="text-sm text-red"></div>
					</div>
				</div>
			</div>

			<!-- DESCRIPTION -->
			<div class="flex flex-col md:flex-row justify-between mb-2">
				<div class=" w-full  text-base">
					{{ form_label(form.description) }}
					{{ form_widget(form.description) }}
					<div class="form-error">
						{{ form_errors(form.description) }}
						<div id="carrierDescription_error" hidden class="text-sm text-red"></div>
					</div>
				</div>
			</div>

			<!-- PRICE -->
			<div class="flex flex-col md:flex-row justify-between mb-2">
				<div class=" w-full  text-base">
					{{ form_label(form.price) }}
					{{ form_widget(form.price) }}
					<div class="form-error">
						{{ form_errors(form.price) }}
						<div id="carrierPrice_error" hidden class="text-sm text-red"></div>
					</div>
				</div>
			</div>


			<!-- SUBMIT BUTTON  -->
			<div class="text-center mt-4">
				<button type="submit" id="carrier_submit" class=" p-2.5 text-sm font-medium text-white bg-primary  border border-primary focus:outline-none focus:ring-primary">
					{{ form.vars.value.id ? 'Update Carrier' : 'Create Carrier' }}
					<span class="sr-only">{{ form.vars.value.id ? 'Update Carrier' : 'Create Carrier' }}
						button</span>
				</button>
			</div>
			{{ form_end(form) }}
		</aside>
	</div>

	<!-- VALIDATE CREATE PRODUCT WITH JS  -->
	<script src="{{ asset('assets/js/admin/createCarrier.js') }}"></script>
{% endblock %}
